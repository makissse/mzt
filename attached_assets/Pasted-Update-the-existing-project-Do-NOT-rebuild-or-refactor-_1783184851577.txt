Update the existing project. Do NOT rebuild or refactor the application. Only apply the changes below while preserving all existing functionality.

====================================================
CRITICAL BUG FIX: MUSIC PLAYER NOT WORKING
====================================================

There is a major issue in the Music playback system:

Problem:
- The music player is not visible at all OR does not render correctly
- Users cannot play audio from uploaded files or external sources

Fix requirements:

- Ensure the music player is properly rendered in all relevant places:
  - Music section in "Мясо 30"
  - Music items inside "Рекомендации"

- The player must support:
  - External audio URLs
  - Uploaded audio files
  - Album tracks playback

- The player must always be visible when audio exists

====================================================
UI / DESIGN REQUIREMENT: MUSIC PLAYER
====================================================

Redesign the audio player UI.

Requirements:

- Must match the overall dark theme of the website
- Must NOT use default white HTML audio player style
- Custom styled player consistent with app design
- Include:
  - Play / Pause button
  - Progress bar
  - Volume control (fully functional and not broken)
  - Time indicator (optional but preferred)

- Player must be:
  - properly sized
  - fully responsive
  - visually integrated into the UI cards

====================================================
FEATURE CHANGE: MUSIC MODE (RECOMMENDATIONS → MUSIC)
====================================================

In the "Рекомендации" mode, update the MUSIC section:

Current behavior:
- Music entries may include too many fields (links, images, etc.)

New simplified structure:

Each music entry MUST contain ONLY:

- Type selector:
  - Single
  - Album / EP

- Title (required)
- Optional description

Remove completely:
- External links field
- Audio URL input field
- Cover image upload field

Important:
- Audio handling must still work internally if required by system
- But user input must be simplified to only the fields above

====================================================
IMPORTANT RULES
====================================================

- Do NOT break existing authentication system
- Do NOT remove other features
- Do NOT redesign the whole app
- Only fix music player + simplify music input form
- Keep both modes ("Мясо 30" and "Рекомендации") working

====================================================
FINAL GOAL
====================================================

Fix the application so that:

- Music player is visible and fully functional
- Audio playback works for all supported sources
- Player has custom dark UI matching the app
- Music creation form in "Рекомендации" is simplified
- No unnecessary fields remain in music creation
- System remains stable and production-ready