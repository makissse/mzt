Update the existing project. Do NOT rebuild or refactor the application. Only apply the changes below while preserving all existing functionality and architecture.

====================================================
CRITICAL UI BUG: MUSIC PLAYER LAYOUT ISSUE
====================================================

There is a visual/layout bug in the music player component.

Problem:
- The toggle switch responsible for switching/scrolling tracks (next/previous or track navigation control) is incorrectly positioned
- It is displayed between:
  - current playback time
  - total duration time
- This makes the UI broken and visually incorrect

Fix requirements:

- Correct the layout of the music player controls
- The toggle switch must be properly positioned and visually aligned with the rest of the controls
- The layout must be clean and consistent with the dark theme
- Ensure spacing between:
  - time indicators
  - progress bar
  - controls
- Do NOT break playback functionality

====================================================
FEATURE CHANGE: MOVIES IN "РЕКОМЕНДАЦИИ"
====================================================

In the "Рекомендации" mode, update the Movies section:

Add a REQUIRED field:

- Genre (жанр)

Requirements:
- Genre must be mandatory when creating a movie entry
- If genre is not provided, submission must be blocked
- Genre must be displayed on movie cards
- Genre must be shown in Russian UI

====================================================
IMPORTANT RULES
====================================================

- Do NOT break authentication system
- Do NOT redesign the entire UI
- Do NOT modify unrelated features
- Only fix the music player layout issue and update movie form validation
- Keep system stable and production-ready

====================================================
FINAL GOAL
====================================================

Fix the application so that:

- Music player layout is correctly aligned and visually clean
- Toggle switch is properly positioned and not overlapping time indicators
- Movie entries in "Рекомендации" require genre field
- Genre is displayed everywhere relevant
- No regressions or broken features