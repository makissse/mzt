Update the existing project. Do NOT rebuild or redesign the application from scratch. Only apply the following changes while preserving all existing functionality.

====================================================
LANGUAGE
====================================================

Translate the entire user interface into Russian.

This includes:

- Navigation
- Buttons
- Forms
- Authentication pages
- Placeholders
- Validation messages
- Modal windows
- Release pages
- Rating section
- Comments
- Empty states
- Error messages
- Success messages
- All UI text everywhere

Everything visible to the user must be in Russian.

====================================================
BRANDING / MODE NAME
====================================================

Rename the sidebar mode:

Current name:
Music Reviews

New name:
Мясо 30

Only change visible text in UI.
Do NOT change internal architecture, folder names, routes, or variables.

====================================================
DESIGN UPDATE (REFERENCE IMAGES)
====================================================

I will provide 3 reference images.

Use them as the main visual direction for redesigning the UI.

Do NOT copy layouts exactly.

Instead, replicate the same visual language:

- colors
- typography
- spacing
- shadows
- borders
- rounded corners
- card style
- button style
- overall aesthetic

====================================================
IMAGE REFERENCES DESCRIPTION
====================================================

Image 1:
Shows how a single release/song card should look.
Use it as the reference for:
- release cards
- and also inspiration for logo typography style

Image 2:
Shows the overall website UI style.
Use it as the reference for:
- layout
- spacing
- structure
- navigation
- general design language

Image 3:
Shows slider UI for rating system.
Use it as reference for:
- rating sliders only
Do NOT change scoring logic.

====================================================
LOGO
====================================================

Redesign the "mzt" logo.

The logo must match the typography style seen in Image 1 and Image 2.

Requirements:
- text-based logo (letters only)
- clean stylized typography
- modern minimal look
- same visual tone as reference images

Do NOT copy directly.
Create a unique "mzt" logo inspired by the same style.

====================================================
BUG FIX (IMPORTANT)
====================================================

There is a bug in the "Single release creation" flow.

Issue:
After filling all required fields for a Single release, the "Add Release" button remains disabled and cannot be clicked.

Important:
- Album / EP creation works correctly and must NOT be broken.
- Only the Single creation flow is affected.

Task:
Find the root cause and fix it.

Requirements:
- The "Add Release" button must become enabled when all required fields for a Single are correctly filled.
- Validation logic must be consistent.
- Do NOT break Album / EP logic.
- Do NOT change unrelated functionality.

====================================================
IMPORTANT RULES
====================================================

- Do NOT rebuild the project
- Do NOT change backend architecture unnecessarily
- Do NOT remove working features
- Only modify UI, text, branding, and fix the Single bug
- Preserve all existing functionality

====================================================
FINAL GOAL
====================================================

The result should be a polished version of the existing app with:

- Full Russian localization
- Updated UI based on reference images
- New logo in matching style
- Mode renamed to "Мясо 30"
- Fixed Single release bug
- No breaking changes